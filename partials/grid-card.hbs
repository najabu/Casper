{{!-- This is a partial file used to generate a post "card"
which templates loop over to generate a list of posts. --}}

<article class="grid-card {{post_class}} {{#is "home"}}{{#if feature_image}}{{#has index="0"}}grid-card-large{{/has}}{{/if}}{{/is}}">

    {{#if feature_image}}
    <a class="grid-card-image-link" href="{{url}}">
        {{!-- This is a responsive image, it loads different sizes depending on device
        https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
        <img class="grid-card-image"
            srcset="{{img_url feature_image size="s"}} 300w,
                    {{img_url feature_image size="m"}} 600w,
                    {{img_url feature_image size="l"}} 1000w,
                    {{img_url feature_image size="xl"}} 2000w"
            sizes="(max-width: 1000px) 400px, 800px"
            src="{{img_url feature_image size="s"}}"
            alt="{{title}}"
            loading="lazy"
        />
    </a>
    {{/if}}

    <div class="grid-card-content">

        <a class="grid-card-content-link" href="{{url}}">
            <header class="grid-card-header">
                {{#if primary_tag}}
                    {{#primary_tag}}
                    <div class="grid-card-primary-tag">{{name}}</div>
                    {{/primary_tag}}
                {{/if}}
                <h2 class="grid-card-title">{{title}}</h2>
            </header>
            <section class="grid-card-excerpt">
                <p>{{excerpt}}</p>
            </section>
        </a>

        <footer class="grid-card-meta">
            <ul class="author-list">
                {{#foreach authors}}
                <li class="author-list-item">
                    {{#if profile_image}}
                    <a href="{{url}}" class="static-avatar">
                        <img class="author-profile-image" src="{{img_url profile_image size="xs"}}" alt="{{name}}" />
                    </a>
                    {{else}}
                    <a href="{{url}}" class="static-avatar author-profile-image">{{> "icons/avatar"}}</a>
                    {{/if}}
                </li>
                {{/foreach}}
            </ul>
            <div class="grid-card-byline-content">
                <span>{{#has author="count:>2"}}Multiple authors{{else}}{{authors}}{{/has}}</span>
                <span class="grid-card-byline-date"><time datetime="{{date format="YYYY-MM-DD"}}">{{date}}</time> <span class="bull">&bull;</span> {{reading_time}}</span>
            </div>
        </footer>

    </div>{{!--/.grid-card-content--}}

</article>
